let initialState = {
    friendsData: [
        { name: 'Mark', id: 1 },
        { name: 'Ann', id: 2 },
        { name: 'Alex', id: 3 },
    ],
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer